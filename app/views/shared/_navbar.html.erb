<ul id="main-nav-menu" class="navbar-nav d-flex">
  <%= render_list_items do |li|
    Homeland.navbar_plugins.each do |plugin|
      li << link_to(plugin.display_name, plugin.root_path, class: "nav-link") if Setting.has_module?(plugin.name)
    end
    li.insert(1, link_to(t("menu.topics"), Setting.has_module?(:home) ? main_app.topics_path : main_app.root_path, class: "nav-link"))
  end %>
  <%= raw Setting.navbar_html %>
</ul>
